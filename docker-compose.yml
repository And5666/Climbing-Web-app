services:
  web:
    build: .
    env_file: .env
    # Laptop-local only: the Cloudflare tunnel reaches this port, the
    # open internet never does.
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - summit-data:/data
    restart: unless-stopped

volumes:
  summit-data:
